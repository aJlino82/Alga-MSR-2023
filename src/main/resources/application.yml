server:
  port: 3000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/db_login?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: suporte
    password: suporte1234
  jpa:
    hibernate:
      ddl-auto: update
